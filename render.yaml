services:
  - type: web
    name: sql-query-feedback-system
    runtime: python
    buildCommand: pip install -r requirements.txt && python database/init_db.py
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
